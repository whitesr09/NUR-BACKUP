# Build boundary

Only source code, assets, and automated source tests are permitted in this development increment. The existing release workflow is not to be dispatched or modified to build automatically on the development branch. No APK is generated, signed, or published. Preserve the original NUR package and signing certificate, user data and custom theme. Final device-dependent features require later verification.
